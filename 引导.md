# Codex 工程接手引导：CR5 · SC3000 · DH-AG95 · SEER AGV

本文既是工程地图，也是给后续 Codex/开发者使用的接手提示词。它说明当前实现的
真实边界、坐标约定、安全约束、模块所有权、测试方法和交付要求。它不能替代设备
厂家手册，也不能把软件变成经过认证的安全控制系统。

## 1. 可直接交给 Codex 的总提示词

复制下面内容作为新会话的第一条工程提示，并在末尾补充本次具体任务：

```text
你正在维护 Ubuntu 22.04 / ROS 2 Humble 下的 Dobot CR5、DH-AG95、
SC3000、AprilTag 2.5D 定位和 SEER AGV 综合上位机。

开始前必须完整阅读仓库根目录的《引导.md》《README.md》《CONTRIBUTING.md》；
若有 AGENTS.md 也必须阅读。再按任务阅读 docs/01～09 以及相关 ROS 包 README。
事实优先级为：当前源码和测试 > 《引导.md》 > 专题文档 > 历史记录。

先执行 git status --short --branch，保留所有现有改动，不 reset、不 checkout 覆盖、
不整树同步。不要提交 build、install、log、points、queues、apriltag_validation、
地图、现场图片、标定结果、设备密码、系统密码、Token、私钥或未脱敏日志。

GUI 只能通过 ROS 2 操作 CR5 和 AGV；只有 seer_agv_node 可以持有 AGV TCP。
任何使能、清警、Continue、关闭保护、机械臂/夹爪运动、API 2003 定位确认或
AGV 非零速度，都必须由现场操作者明确授权。离线测试不得连接或驱动真机。
不自动重发结果不确定的运动命令，不调用会堵塞官方驱动的 Sync()，不通过删除
安全门、自动清警或扩大阈值掩盖设备故障。

修改后运行文档规定的完整构建和离线回归，报告真实 passed 数量、是否连接真机、
是否发生运动、接口/schema 变化、修改文件和剩余风险。不要把只跑新增用例称为
完整回归。

本次任务：<在这里写清目标、允许的真机操作和验收标准>。
```

## 2. 项目目标、真值来源与边界

项目用一个 PyQt5 上位机集成以下能力：

- CR5 连通性、模式、报警、六轴角和 Tool0 位姿；
- 使能/下使能、低速运动、拖动取点和保护恢复；
- 末端 RS485 DH-AG95 初始化、夹持力、开度和反馈；
- SC3000 FTP 收图、Modbus TCP 触发、约 5 FPS 临时预览和同步采集；
- 棋盘格内参、AprilTag `solvePnP`、眼在手上手眼标定及基座坐标输出；
- SEER 地图、定位、站点/本地航点、路径预览、导航和低速点动；
- AGV、机械臂、夹爪、等待和 AprilTag 到站纠偏的串行任务队列。

源码与测试是实现真值。`docs/10-project-history.md` 和厂家原 README 只用于了解
历史，里面可能出现旧无线 IP、反馈端口 30004 或旧界面结构，不应覆盖当前配置。
现场 IP、地图、相机内参、手眼外参、Tool/User 坐标和负载均为部署数据，不能把
一台设备上的值照搬到另一台设备。

## 3. 总体架构和通信所有权

```text
PyQt5 综合上位机
 ├─ DobotRosClient ─ROS服务→ dobot_bringup_v3 ─29999/30003→ CR5
 │                                      └─30005实时帧→ 关节/Tool0话题
 │                                                   └─末端RS485→ DH-AG95
 ├─ Sc3000CameraCapture ─Modbus TCP 502→ SC3000
 │                       ←FTP 2121/30000:30009上传JPG
 └─ /seer_agv/* ROS接口→ seer_agv_node（AGV TCP唯一所有者）
                         └─19204/19205/19206/19207→ SEER AGV
```

必须保持以下边界：

- GUI 不直接创建 `SeerClient`，也不持有 AGV socket；
- CR5 接口位于 `/dobot_bringup_v3/*`，AGV 位于 `/seer_agv/*`；
- AGV 点动只发布 `/seer_agv/cmd_vel`；TF 使用 `seer_map -> seer_base_link`，不能
  污染 CR5 的 `base_link`；
- ROS 驱动运行时，不允许 DobotStudio、`nc` 或第二份程序并发占用 CR5 控制端口；
- SC3000 FTP Server 必须单实例监听，避免同一端口和上传文件的所有权冲突。

## 4. 当前硬件和网络基线

| 对象 | 当前配置 |
|---|---|
| Ubuntu PC | `192.168.192.104/24`；机器人有线网络不设默认网关 |
| CR5 | `192.168.192.201`；Dashboard 29999、运动 30003、实时反馈 30005 |
| 兼容反馈 | 30004 只用于其他控制器；本项目 CR5 上会握手但不持续发送反馈 |
| SC3000 | `192.168.192.11`；Modbus TCP 502；向 PC FTP 2121/30000:30009 上传 |
| SEER AGV | `192.168.192.5`；状态/控制/导航/配置为 19204/19205/19206/19207 |
| DH-AG95 | CR5 末端 RS485；Modbus RTU 从站 1，115200 bit/s，8N1 |

CR5、SC3000、SEER 和 PC 同属 `192.168.192.0/24`。Wi-Fi 可用于互联网，但
VPN、Mihomo、Meta 或 TUN 不得接管机器人子网。部署时先动态确认真实网卡名：

```bash
ip -br link
ip -br addr
ip route get 192.168.192.201
ip route get 192.168.192.11
ip route get 192.168.192.5
```

三条路由均应显示真实有线接口和 `src 192.168.192.104`。`nc` 成功只说明 TCP
握手，代理可能造成假阳性；还要核对 carrier、路由和真实状态/反馈字节。

关键环境变量：

```bash
export DOBOT_WS="${HOME}/dobot_ws"
export DOBOT_TYPE=cr5
export IP_address=192.168.192.201
export DOBOT_FEEDBACK_PORT=30005
export SC3000_IP=192.168.192.11
export SC3000_FTP_USER='<现场FTP用户>'
export SC3000_FTP_PASSWORD='<现场FTP口令>'
export SEER_AGV_HOST=192.168.192.5
# 仅在自动路由识别不适用时设置：
# export SEER_AGV_INTERFACE='<实际有线网卡>'
```

`DOBOT_FEEDBACK_PORT` 仅允许 30004 或 30005，未设置时默认 30005；非法字符串或
其他端口必须显式失败。生产部署必须更换示例 FTP 凭据并通过环境变量传入，不能
把真实口令写入 Git。

## 5. 仓库结构与关键文件

| 路径 | 职责 |
|---|---|
| `src/DOBOT_6Axis_ROS2_V3` | 厂家 ROS 2 V3 驱动、消息、模型和 MoveIt；含本项目连接恢复、报警解析、Modbus、SafeSkin、反馈帧修复 |
| `src/dobot_point_manager` | 默认只预览、显式 `--execute` 的低速关节点运动 CLI；不使用 `Sync()` |
| `src/dobot_operator_gui` | 综合 GUI、ROS 门面、点位、相机、标定、视觉纠偏和队列 |
| `src/seer_agv_driver` | SEER TCP 协议、唯一 TCP 所有者、状态安全门、地图/TF/导航/点动 |
| `src/seer_agv_msgs` | 地图、重定位、定位确认、导航和路径预览 ROS 服务 |
| `start_dobot_operator_gui.sh` | 清理 Conda/Snap/Qt 环境、复用或启动唯一 AGV 驱动、启动 GUI、退出停止 |
| `estimate_apriltag_pose.sh` | 单帧 AprilTag 定位快捷入口 |

`dobot_operator_gui` 的重要实现文件：

- `main_window.py`：界面、后台任务、人工确认、关闭/失败恢复；
- `ros_client.py`：CR5、夹爪和 AGV 的类型化 ROS 门面与状态机；
- `core.py`：点位校验、解析、原子保存和 TCP 辅助检查；
- `camera_capture.py`：FTP Server、Modbus 触发和完整图片检测；
- `apriltag_localization.py`：标签检测、IPPE 候选、LM 精修和叠加显示；
- `handeye_transform.py`：Tool0、相机、标签刚体变换；
- `visual_correction.py`：多帧统计、纠偏矩阵和目标限幅；
- `task_queue.py`：版本化 JSON、全量严格校验和串行执行；
- `agv_map_widget.py` / `agv_waypoints.py`：统一地图和本地航点。

当前 GUI 是五个页签：

1. 连接、状态与日志；
2. 机械臂与 DH-AG95；
3. 取点与点位运动（含相机实时画面）；
4. 任务队列；
5. SEER AGV 综合控制（状态、统一地图、加载/重定位、航点、导航、点动）。

## 6. CR5 驱动、模式和运动安全逻辑

`core.py` 中的当前模式解释为：1 初始化、2 抱闸松开、3 未上电、4 未使能、
5 已使能且空闲、6 拖动、7 运行、8 单次运动/Jog、9 报警、10 暂停、11 碰撞。
不要只看模式数字决定动作，还要读取报警和真实位置。

主要 ROS 服务前缀是 `/dobot_bringup_v3/srv/`，包括 `RobotMode`、`GetAngle`、
`GetPose`、`GetErrorID`、`ClearError`、`EnableRobot`、`DisableRobot`、
`SpeedFactor`、`JointMovJ`、`MovJ`、`MovL`、`StartDrag`、`StopDrag`、
`SetCollisionLevel`、`SetSafeSkin`、`Continues`、`EmergencyStop` 和 Modbus 服务。

重要语义与实现：

- 控制器响应 `res=0` 只说明 API 请求被接受或完成了协议处理，物理动作仍需用模式、
  位置或末端反馈确认；
- 反馈节点从 30005 重组完整 1440 字节帧、校验 magic，断线时关闭 socket 并定时
  重连，发布 `/joint_states_robot` 和 `/dobot_msgs_v3/msg/ToolVectorActual`；
- Dashboard 拒绝 `GetAngle/GetPose` 时，GUI 可用不超过 2 秒的新鲜实时反馈兜底；
  过期反馈必须报错，`RobotMode` 仍以 Dashboard 为准；
- `EnableRobot` 使用专用 30 秒服务超时，`res=0` 后再等待最多 8 秒确认模式 5；
  只有模式 5 成立才设置速度。若仍为模式 4，显示模式和报警并提示检查实体急停；
  报警 116 应处理物理急停/上电根因，不能靠反复清警或无限加超时解决；
- 点位运动必须从模式 5 开始。命令后轮询真实位置和模式，连续稳定到位才成功；
  约 10 秒无变化判“受理但未开始”，报警/异常模式立即失败；
- 绝不使用 `Sync()`。厂家 bringup 的单线程回调会在 socket `recv()` 上堵住，
  客户端超时无法取消服务端阻塞；发生过该问题应停止整个 launch 后重启；
- socket 返回不确定时绝不自动重发运动命令，否则可能造成重复运动；
- 模式 10 的 `Continue` 可能恢复旧队列，只能在报警为空且操作者确认路径安全后
  显式执行；`ClearError` 不消除报警根因，也不得自动调用。

拖动进入时序：模式 5 → 按操作者勾选临时降低碰撞等级/关闭 SafeSkin →
`StartDrag` → 确认模式 6；失败时按相反方向回滚。退出时序必须是：

```text
模式6时仅发送一次 StopDrag
→ 轮询并确认控制器稳定到模式5
→ SetSafeSkin(1)（若本程序曾关闭）
→ 恢复碰撞等级（若本程序曾修改）
```

控制器未稳定到模式 5 时禁止恢复保护参数，否则可能触发控制模式切换报警 161。
已经是模式 5 时不得重复 `StopDrag`。SafeSkin 和本体碰撞检测是两套独立保护，
默认都应开启；`-3` 是 SafeSkin 触发，不是网络故障。

## 7. 点位和 DH-AG95

点位兼容旧式文件 `points/P1_joint.txt`、`P1_pose.txt`，也支持同步采集组：

```text
points/P1/
├── P1_joint.txt
├── P1_pose.txt
├── P1_image.jpg
└── P1_capture.json
```

点名只允许字母、数字、点、下划线和连字符；采用临时文件和原子替换保存。同步
采集会在曝光前后读取关节、Tool0 和模式，默认关节漂移超过 0.05° 时拒绝保存。
这些点位、图片和标定结果属于运行数据，默认被 `.gitignore` 排除。

DH-AG95 经控制器内部 `127.0.0.1:60000` 建立 RTU 桥，关键寄存器：

| 寄存器 | 方向 | 含义 |
|---|---|---|
| `0x0100` | 写 | 初始化，写 1 |
| `0x0101` | 写 | 夹持力 20～100% |
| `0x0103` | 写 | 目标位置；0 闭合，1000 打开 |
| `0x0200..0x0202` | 读 | 初始化、夹持状态和实际位置 |

界面“闭合比例”不是角度：0% 为全开、100% 为全闭，转换为
`position = 1000 × (100 - percent) / 100`。队列必须等待反馈；状态 1 到位或状态 2
夹到物体才继续，状态 3 表示物体脱落。VX500 与 DH-AG95 不能同时占用末端 RS485。

## 8. SC3000、内参、AprilTag 和手眼变换

Linux 路线不依赖 Windows SCMVS SDK。相机 Modbus 触发顺序：

```text
写 REG0=1 → 等 REG1.bit0 Ready
→ 写 REG0=3 触发
→ 等 REG1.bit8 Results Available 或 bit9 Timeout
→ 写 REG0=5 Ack
→ 写回 REG0=1
```

相机必须已进入循环运行态，FTP 目标指向 PC。实时预览目标周期 200 ms（约 5 FPS，
不是硬实时），帧写入 `/dev/shm/dobot_operator_gui_sc3000`，显示后删除；只有用户
执行同步采集时才把图片持久保存到选择的点位目录。

视觉算法约定：

- 棋盘为 8×6 方格、7×5 内角点、格边 11.2 mm；先用
  `findChessboardCornersSB`，必要时回退传统检测和 `cornerSubPix`，再用
  `calibrateCameraExtended`、逐图重投影误差和留一法比较模型；
- 当前标签为 AprilTag `tag36h11`，黑框真实边长 58.5 mm；检测四角后以
  IPPE Square 生成候选，选择正深度且重投影误差最小者，再 LM 精修；
- 相机坐标为 X 向图像右、Y 向图像下、Z 沿镜头光轴向前；标签尺寸写错 10 倍会
  让估计 XYZ 近似错 10 倍；
- 眼在手上标定要求相机固定末端、标定板/标签固定，机械臂用多个具有足够旋转
  激励的姿态采集。支架、焦距、分辨率、Tool/User 改变后必须重新标定。

矩阵命名统一为 `destination_T_source`。核心链路为：

```text
base_T_tag = base_T_tool0 × tool0_T_camera × camera_T_tag
base_T_grasp = base_T_tag × tag_T_grasp
```

默认程序会从 `points/calibration_results/` 查找内参和手眼文件，但它们是当前设备/
支架专属运行数据，不应上传。通用的 `tag_T_grasp` 抓取偏移尚不是自动推导项，必须
按具体任务示教/标定，不能声称“看到标签即可自动抓取”。

## 9. SEER AGV 地图、定位、导航和点动

`seer_agv_node` 是唯一 TCP 所有者。主要 ROS 话题包括：

- `/seer_agv/status`（JSON）、`/pose`、`/odom`、`/cmd_vel`；
- `/seer_agv/stations`、`/map_data`、`/map_markers`、`/footprint_markers`。

主要服务包括 `/stop`、`/download_map`、`/load_map`、`/relocalize`、
`/cancel_relocalization`、`/confirm_localization`、`/navigate_to_station`、
`/navigate_to_pose`、`/plan_to_station` 和 `/cancel_nav`。

运动安全门要求连接和状态新鲜、地图已加载、定位已确认、无急停/充电/控制权
占用/blocked/slowed/活动报警，且导航和点动互斥。当前 Robokit 3.4.4.6 中
`reloc_status=3` 表示等待操作者确认；程序绝不自动调用 API 2003。确认时 GUI 带上
操作者看到的 X/Y/Yaw 快照，驱动再次验证车辆静止、状态新鲜、位置变化不超过
0.05 m/5°，先确认停车再发 2003。

点动只允许差速底盘 `vy=0`，并限制 `|vx|≤0.1 m/s`、`|w|≤0.2 rad/s`。按钮按住
期间每 100 ms 续发，释放立即发零速度；驱动约 0.4 s 看门狗会停止。若“前进无
反应”而 `slowed=true`，是前向安全区在正常拦截，应排查障碍和传感器，不能删除
安全门。

统一地图画布同时显示地图、车体轮廓和朝向、官方站点、本地航点及路径。本地航点
保存在 `${DOBOT_WS}/agv_waypoints.json`，按地图名隔离，字段为 `name/x/y/yaw`，
不会写入 Robokit 地图。官方站点和本地目标均支持：

- `auto`：不强制底盘领行方向；
- `forward`：车头领行；
- `backward`：车尾领行。

目标 Yaw 决定最终朝向，`travel_mode` 只决定行驶方向。官方站点调用 3051 时，
auto 省略 `method`；正走/倒走显式传 `forward/backward`。本地 goPath 任务中 auto
省略 `backMode`，正走/倒走传 0/1。旧队列没有该字段时按 auto 读取。

## 10. 任务队列与 AprilTag 到站纠偏

队列 schema 版本为 1，当前动作 `kind` 为：

```text
agv_navigate_station
agv_navigate_pose
move_point
measure_apriltag_correction
move_point_corrected
gripper_close_percent
gripper_position
gripper_force
gripper_initialize
wait
```

加载时一次性严格校验全部字段、范围和点位，随后严格串行执行；前一步物理完成后
才进入下一步，任何失败都会停止所有后续动作。用户航点队列项保存完整 XY/Yaw
快照，同名航点后来被编辑不会悄悄改变旧队列。每次 AGV 导航都会立即清除上一轮
视觉纠偏。

纠偏不是把 XYZ 差值加到关节角。理想到站时保存固定标签参考位姿
`B_ref_T_tag`，实际到站多帧求 `B_now_T_tag`：

```text
correction = B_now_T_tag × inverse(B_ref_T_tag)
B_now_T_target = correction × B_ref_T_target
```

平移采用多帧中值，旋转矩阵平均后通过 SVD 投影回 SO(3)。同一次纠偏可应用到
接近、按压、撤回等多个 Tool0 笛卡尔目标。默认门限为 3 帧、重投影误差≤1.5 px、
纠偏平移≤50 mm、旋转≤5°、多帧离散≤2 mm/1°，同时检查旋转杠杆效应造成的
每个目标实际位移。模式不是 5、机械臂移动、内参/手眼无效、标签不唯一或超过
门限时必须失败关闭。

推荐的按钮任务顺序：安全运输位 → AGV 低速导航 → 普通关节运动到宽视野观察位
→ 测量 AprilTag 纠偏 → MovJ 到接近位 → 低速 MovL 按压 → 等待 → 低速 MovL
撤回。验收应按“只测量预览 → 接近位 → 空行程 → 真实按钮”逐级进行。

## 11. 构建、启动和离线验证

ROS 2 Humble 使用系统 Python 3.10，不为本项目创建 Conda 环境：

```bash
export DOBOT_WS="${HOME}/dobot_ws"
cd "${DOBOT_WS}"

conda deactivate 2>/dev/null || true
unset CONDA_PREFIX CONDA_DEFAULT_ENV CONDA_PROMPT_MODIFIER
unset PYTHONHOME PYTHONPATH
export PYTHONNOUSERSITE=1
export PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

source /opt/ros/humble/setup.bash
rosdep install --from-paths src --ignore-src -r -y
colcon build --symlink-install --packages-up-to \
  seer_agv_driver dobot_operator_gui dobot_point_manager
source "${DOBOT_WS}/install/setup.bash"
```

终端 A 启动 CR5 驱动：

```bash
source /opt/ros/humble/setup.bash
source "${DOBOT_WS}/install/setup.bash"
export DOBOT_TYPE=cr5
export IP_address=192.168.192.201
export DOBOT_FEEDBACK_PORT=30005
ros2 launch dobot_bringup_v3 dobot_bringup_ros2.launch.py
```

终端 B 启动综合上位机：

```bash
"${DOBOT_WS}/start_dobot_operator_gui.sh"
```

启动脚本不会自动使能 CR5 或发送非零 AGV 速度。AGV 可达且没有现有
`/seer_agv_node` 时才启动唯一驱动，否则复用现有节点或在无 AGV 模式下继续。

提交前必须执行 `docs/09-testing-troubleshooting.md` 列出的完整测试，并额外检查：

```bash
bash -n start_dobot_operator_gui.sh estimate_apriltag_pose.sh
python3 -m py_compile \
  src/dobot_operator_gui/dobot_operator_gui/*.py \
  src/seer_agv_driver/seer_agv_driver/*.py
git diff --check
```

协议测试使用假 socket/响应，不应连接设备。需要 GUI smoke 时使用
`QT_QPA_PLATFORM=offscreen`。测试数量会随用例增长，必须报告本次 `pytest` 的
真实结果，不能沿用文档中的旧数字。

## 12. 真机测试的授权和顺序

没有操作者明确授权时只允许离线或只读检查。严禁自动使能、自动清警、Continue、
关闭安全保护、运动、夹爪闭合、API 2003 或 AGV 非零速度。

获得明确授权后也必须逐阶段验收：

1. 接线、电压、总负载、工具、线缆、实体急停和完整运动包络；
2. 有线 carrier、IP、路由、ping 和端口；
3. 只读模式、角度、Tool0、报警和 AGV 状态；
4. 静止状态下检查相机、AprilTag 和地图；
5. 夹爪低力、空载、短行程；
6. CR5 低速、短距离、单方向运动；
7. 拖动取点及 SafeSkin/碰撞保护恢复；
8. AGV 低速单方向点动；
9. 站点导航；
10. 最后才运行视觉纠偏和组合队列。

## 13. 常见故障速查

- `install/setup.bash` 不存在或 package not found：先成功构建，再 source 正确工作区；
  切换目录本身不会安装包。
- service type invalid：通常是接口包未重建、未 source 最新 install 或混用了工作区；
  用 `ros2 service type`、`ros2 interface show` 核对。
- `ConnectionResetError 104`：并发客户端、链路或控制器断开；停止整个 launch 后
  重启，不能继续使用已经死亡的服务。
- feedback 报 `feed_v` 不存在：运行了旧 install；源码已用 `None` 防护和重连，
  重新构建并启动。
- 30005 反复 timeout：握手不等于反馈流；检查真实路由、TCP-IP 服务、端口是否被
  占用和是否收到完整帧，绝不能伪造关节数据。
- `/joint_states_robot` 全 0：核对 `DOBOT_FEEDBACK_PORT=30005`、magic、完整帧和
  当前安装；与 `GetAngle` 对比。
- `Sync` 超时：停止并重启 bringup，改用轮询式运动；不要只增加超时。
- EnableRobot 超时/模式 4：运行最新版 30 秒逻辑并处理急停、上电和报警根因；
  不能反复使能。
- 模式 9 清警后仍为 9：报警根因仍存在；`res=0` 不等于报警消失。
- 模式 10：可能有旧运动队列，必须现场确认后才 Continue。
- 模式 11：先排除真实接触/碰撞，不能关闭保护绕过。
- 报警 161：退出拖动恢复保护过早；按第 6 节严格时序，已产生时按厂家要求处理。
- SC3000 无图：依次查相机循环态、FTP 目标、502、2121、被动端口、防火墙、凭据
  和单实例 FTP。
- `AMENT_TRACE_SETUP_FILES` 未绑定：source ROS 前不要启用 `set -u`。
- AprilTag 深度错约 10 倍：核对 58.5 mm 是否误写 5.85 mm，以及图像分辨率/内参。
- `/seer_agv/download_map` 不可用：检查节点、工作区 source/构建、ROS graph 和
  `/seer_agv/status`，不要让 GUI 直接连第二份 TCP。
- `localization awaiting operator confirmation`：状态 3，现场核对后用受保护的
  2003 确认，绝不自动确认。
- AGV 前进无反应：比较发送 vx、实测 vx 与 `slowed`；安全区拦截时清除障碍，不
  修改驱动绕过。
- GUI 打不开：查看 `~/.ros/dobot_operator_gui_launcher.log`、重复进程和 Conda/
  Snap Qt 污染，优先通过根启动脚本运行。

## 14. 修改、测试和 Git 交付规则

开始修改：

1. 阅读本引导、相关文档、包 README 和当前测试；
2. 执行 `git status --short --branch`，确认并保留用户改动；
3. 用 `rg` 定位真值，用小范围补丁修改；不要整树覆盖运行工作区；
4. 新动作能力必须覆盖正常、超时/断线、畸形响应、安全拒绝和取消/关闭恢复；
5. 接口或 JSON schema 改动要向后兼容，或提供明确迁移与失败提示；
6. 先完成离线全回归，再按授权范围决定是否真机测试。

公共提交前再次确认没有：账号密码、Token、`.env`、私钥/证书、点位、图片、地图、
标定文件、队列、日志、缓存和构建产物。厂家驱动与模型的权利归属不能由本项目
擅自重新许可；仓库尚无统一根许可证时，应先核对上游授权，再决定再分发方式。

每次交付至少报告：

- 问题与根因；
- 修改的文件；
- ROS 接口或队列 schema 是否变化；
- 构建命令与完整回归的真实 passed 数量；
- 是否连接真机、是否使能、是否发生机械臂/夹爪/AGV 运动；
- 日志位置、剩余风险和回滚方法。

## 15. 阅读索引

- `docs/01-hardware-network-safety.md`：网络、端口和安全；
- `docs/02-install-build-run.md`：依赖、构建和启动；
- `docs/03-cr5-operation.md`：CR5、拖动和点位运动；
- `docs/04-sc3000-vision-calibration.md`：相机、PnP 和手眼；
- `docs/05-dh-ag95-and-queue.md`：夹爪、队列和视觉纠偏；
- `docs/06-seer-agv.md`：地图、定位、导航和点动；
- `docs/07-api-reference.md`：ROS 与设备 API；
- `docs/08-code-inventory-development.md`：源码职责；
- `docs/09-testing-troubleshooting.md`：回归命令和故障排查；
- `docs/10-project-history.md`：历史背景，不作为当前配置真值；
- `docs/11-github-release-reuse.md`：发布、许可证和复用；
- `docs/12-source-manifest.md`：文件清单。
